# Copy this file to .env for local development.
# Keep real credentials and machine-specific paths out of Git.

# Optional provider credentials. Leave empty when using public or CSV-only data.
TUSHARE_TOKEN=

# Optional runtime profile for local scripts or future services.
STOCK_PICKER_ENV=local
